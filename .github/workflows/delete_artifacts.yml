name: Remove old artifacts

on: [push]

jobs:
  remove-old-artifacts:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Remove old artifacts
      run: python .github/workflows/delete_artifacts.py ${{ secrets.GITHUB_TOKEN }}