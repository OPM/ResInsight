name: Remove old artifacts

on: [push]

jobs:
  remove-old-artifacts:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Remove old artifacts
      run: |
        pwd
        dir
        cd scripts/githubAction
        dir
        #pwd
        #python delete_artifacts.py ${{ secrets.GITHUB_TOKEN }}
